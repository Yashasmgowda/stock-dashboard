In order to run  - open your IDE Terminal
1 - run - 
cd stock-dashboard
2 - (npm install
     npm install -D tailwindcss postcss autoprefixer
     npm install recharts lucide-react

    - npx tailwindcss init -p) run these comands in your terminal if these directories are not installed 

3 run - npm run dev
